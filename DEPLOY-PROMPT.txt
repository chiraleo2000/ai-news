Deploy today's AI news to GitHub Pages now. Follow skill ai-news-github-deploy (v9).

Hard rules:
- Do NOT ask questions or confirmation. Finish in one run.
- Do NOT use computer-use / desktop / GUI.
- Use Shell/PowerShell only on Windows Cursor (NOT Linux /sessions sandbox).
- Push branch master only.
- NEVER stage or commit: *.ps1, *.bat, *.bundle, .github-pat, .push-trigger, TRIGGER-PUSH.txt, logs, batches, reports.
- NEVER put a PAT into a committed file. If auth needs .github-pat, use it only for git push, then restore remote to https://github.com/chiraleo2000/ai-news.git.
- NEVER write .push-trigger or report PUSH_TRIGGERED. Retry push until SUCCESS, or report BLOCKED/ERROR.
- NEVER tell me to run git push myself.

Steps (must all run):
1. cd C:\Users\AI-PC-DEV\Documents\ai-news (git rev-parse --show-toplevel).
2. Clear .git/**/*.lock files.
3. $today = Get-Date -Format yyyy-MM-dd.
4. Copy Document/${today}_News/${today}_news.json → data/${today}_news.json.
5. If github-pages/data exists, copy the same JSON there.
6. Prepend ${today}_news.json onto data/manifest.json (and github-pages mirror) without duplicates.
7. git add ONLY: data/, github-pages/data/, Document/${today}_News/, Document/manifest.json.
8. Commit: Update AI news ${today} (skip commit if nothing changed).
9. git push origin master (retry up to 5x).
10. If non-fast-forward: git pull --rebase origin master then push again.
11. Verify HEAD == origin/master.
12. If sandbox has no GitHub network: report BLOCKED (do not invent triggers).

Final reply must be exactly one of:
- SUCCESS ✅ — pushed to GitHub — https://chiraleo2000.github.io/ai-news/
- SUCCESS ✅ — nothing new to commit
- BLOCKED: sandbox has no GitHub network — re-run this skill in Cursor on Windows to push.
- ERROR: missing source Document/{date}_News/{date}_news.json
